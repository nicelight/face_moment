# Papercut

- Shell inspection command failed because a redirection was placed inside a `for` command's file-list expansion; rerun with the glob/list guarded outside the command substitution.
- `playwright cli open` rejected the guessed `--headless` option; the installed CLI defaults were used instead.
- The host Python environment lacks `pytest`; the native central-shell regression could not run and was recorded as unavailable rather than treated as a source failure.
- The Compose central-shell pytest gate passed but emitted the known non-failing `/app/.pytest_cache` permission warning from the non-root image user.
