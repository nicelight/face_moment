# Evidence-backed papercut

- The long-lived `realtime`/`backend` service containers continued running a
  pre-rebuild image after `docker compose build`; an earlier receipt also
  proved that the old image lacked the current promo session orchestration
  source. This forced all accepted TASK-075 gates to use disposable
  `docker compose run` containers plus an explicit rebuilt-image/source-hash
  marker. The implementation did not restart the long-lived services.
