# Papercuts

- The host environment has no `python` executable; focused Python probes must
  use the project container or an explicitly provisioned interpreter.
- A multi-file patch with an empty update hunk failed atomically; split source
  patches into independently valid edits before retrying.
