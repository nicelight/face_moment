# Papercuts

- The disposable Compose migration run reached Alembic revision `0010` but the `migrate` entrypoint then failed its unrelated MinIO bucket bootstrap because the isolated project had only started PostgreSQL; start the disposable `minio` service before rerunning migration-backed probes.
- A read-only inline Python metadata probe used a compound `with` statement after semicolons in `python -c`, so it failed at syntax parsing before importing or accessing the database; keep one-line container probes syntactically flat or use a stdin script without creating a file.
- A broad `rg` scope scan named not-yet-created brownfield capability directories and treated exit 2 as a clean branch; restrict scans to existing paths and distinguish staff-session `token_hash_sha256` from display-client symbols.
