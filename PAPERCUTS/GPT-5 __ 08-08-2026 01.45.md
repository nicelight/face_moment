# Papercuts

- A combined read of several mandatory planning documents exceeded the tool output budget and was truncated, forcing smaller repeat reads to guarantee complete priming.
- The environment has `python3` but no `python` executable, so a non-essential interpreter capability check failed after the required Node validator had already been found.
- A first custom queue check treated Foundation's valid special requirement ID `REQ-000` as malformed because it assumed every requirement had a category segment; the check had to be corrected and rerun.
