# Papercuts

- A multi-file `apply_patch` failed because one expected protocol context line
  differed from the current artifact. No files were changed; reread the
  protocol before retrying the smaller patch.
