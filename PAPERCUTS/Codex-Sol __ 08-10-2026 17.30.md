# Papercuts

- The task's initialized `verification.md` omitted the frontmatter implied by
  the framework template, so the first exact-context patch failed and required
  a read/retry against the actual task-owned file shape.
- The repository host has `python3` but no `python` executable and lacks the
  package's `cv2` dependency; importing a dependency-free inventory submodule
  still eagerly loads package `__init__`, so a lightweight read-only probe
  required direct module loading after two immediate environment retries.
