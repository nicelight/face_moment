# Papercuts

- The repository Compose manifest is `compose.yaml`, not the conventional
  `docker-compose.yml`; an initial static-inspection command used the latter
  and stopped before reading the intended files.
- A broad protocol/report patch did not apply because a copied command-result
  paragraph had drifted; use narrower, current-context patches for durable
  evidence updates.
