---
description: Minor workflow friction observed during the FT-001 planning session.
---

- `git log -- .protocols/FT-001/plan.md` fails with `no such path ... in HEAD`
  even though the operational file exists, because `.protocols/` is ignored;
  inspect its filesystem content/metadata instead of assuming Git history is
  available for that planning evidence.
- In a `jq` array, filters such as `.evidence_required|length` need explicit
  parentheses when followed by sibling expressions; otherwise pipe precedence
  can feed the resulting number into the next field lookup.
