# EXIF regression gate papercuts

- Existing helpers in `tests/processing/test_photo_orchestration.py` and
  `tests/processing/test_terminal_publication.py` omitted required
  `Photo.admission_pipeline_revision_id`, so nine existing cases failed at
  fixture insertion before exercising processing. The assigned regression work
  adds the explicit revision in those two helpers.
- `tests/processing/test_terminal_publication.py` uses the configured database,
  calls Alembic upgrade and cleans all stale fixtures selected by
  `Spa.name LIKE 'task-023-%'`. The first focused invocation accidentally ran
  this module with `.env.local` directly; a claim that the whole session was
  isolated would be incorrect. Later gates use the evidence-owned disposable
  database/bucket wrapper. See `.tasks/ASTRA-findings/06-exif/implementation-report.md`
  for the exact possible effects and evidence limits. No operator database
  investigation or cleanup was performed after discovering this error.

## UNSAFE OUT-OF-SCOPE DATABASE TEST EXECUTION DURING ASTRA FINDING 9

- The finding 9 executor ran `docker compose run --rm backend python -m pytest -q --disable-warnings` outside the assigned JavaScript scope.
- The command exited with status 1; no complete log was retained.
- The finding 9 executor rebuilt `face-moment:dev` at local `2026-09-07
  03:18:16`; root confirmed the timestamp via read-only Docker metadata.
- The finding 9 executor recreated the PostgreSQL and MinIO containers for the
  main `face-moment` project at local `2026-09-07 03:19:22` and ran migrations
  at local `2026-09-07 03:19:24` against existing volumes created `2026-07-24`;
  root confirmed these timestamps via read-only Docker metadata.
- The ephemeral `backend-run` container was removed. No `docker compose down`
  or volume deletion was performed.
- No before-snapshot was captured, so data impact is unknown.
- Root halted the executor and requested a read-only fixture audit. This
  record contains no user data, credentials or tokens.

## Packaged smoke network preflight

- Docker standard networks may expose `IPAM.Config: null`. The initial finding 10 smoke reader treated it as iterable and stopped before creating resources. Fixed by treating null as an empty list; the next full smoke passed. Both logs are retained under `.tasks/ASTRA-findings/10-packaged-smoke/`.

## TASK-110 verification tooling warnings

The task-scoped disposable pytest runs emit Alembic's existing `path_separator`
configuration deprecation warning; the required Memory Bank lint passes but
recommends `source_of_truth` in the existing TASK-101 bug document. Neither
warning blocks TASK-110. Evidence: `.tasks/TASK-110-T3-FT-012-W3/execution-gates.log`
and `mb-lint-execution.log`. No unrelated configuration/doc repair was added.

## TASK-110: purge missed derivatives from interrupted processing

Independent verification reproduced a processing crash after deterministic media
publication but before database publication. The existing cleanup read only
stored references, so completed purge left two private derivatives. Current
TASK-110 repair extends the existing processing-owned cleanup to resolve missing
references via the established Photo/revision deterministic keys. The supported
crash path is now a permanent inventory purge regression; original failure is
retained in `.tasks/TASK-110-T3-FT-012-W3/verify-unpublished-media.log`.
