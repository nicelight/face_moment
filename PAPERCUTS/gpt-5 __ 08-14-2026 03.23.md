# Papercuts

- Initial tier-policy section lookup used outdated anchor spelling, causing a non-actionable command failure; read the policy directly instead.
- Initial task-index status query assumed an array; this index is an object map, so the query was corrected before preflight decisions.
- One large protocol/artifact patch had stale context and did not apply; subsequent task-owned updates were split into smaller patches.
- The mounted source test initially imported the stale installed image package; `PYTHONPATH=/app/src` was necessary for source-level proof while retaining a separate packaged-image build request.
- `mb-lint` passes but prints an unrelated active FT-001 metadata recommendation; recorded it as non-task-owned evidence rather than modifying that feature.
- A second multi-file protocol patch repeated a one-occurrence context line and did not apply; verification and handoff updates were split again.
- Packaged pytest passed but emitted a non-failing `PytestCacheWarning`: the non-root image user cannot create `/app` cache files. The gate remains reproducible without cache writes.
- A packaged-handoff patch used an outdated wording for the build note and did not apply; the task-owned report was refreshed from the current file instead.
