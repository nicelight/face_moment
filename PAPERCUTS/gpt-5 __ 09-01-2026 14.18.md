# Papercuts

- `tests/promo/test_promo_display_outcome.py::test_backend_exposes_exact_authenticated_ack_route_and_response` uses a fake `DatabaseSession` without the `scalar`/`get` methods that the committed `src/face_moment/promo/http.py` already calls after display acknowledgement. The focused FT-009 tests and required gates pass, but the broader Promo regression exposes this pre-existing fixture/code mismatch (`git show HEAD` contains the lookup). The owning Promo test-maintenance task should reconcile the fixture without changing display behavior.
