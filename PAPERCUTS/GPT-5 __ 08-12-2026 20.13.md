- A first effective-topology `jq` projection changed filter context inside an
  `all(...)` expression and failed with `Cannot index string with string
  "services"`; capture the root document before iterating service names.
