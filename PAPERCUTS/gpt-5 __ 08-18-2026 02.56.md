# Workflow papercuts

- The current tool surface did not expose the delegated-agent spawn helper,
  although the project workflow documents expect fresh child contexts. The
  bounded TASK-045 runtime-provisioning stop was therefore reconciled directly
  in the top-level task protocol without performing any runtime mutation.
