# Existing inventory selector assertion is stale

The isolated PostgreSQL regression run for venue detector controls passed 25 of
26 cases. `tests/serving_control/test_active_search_date.py::test_spa_rename_persists_in_staff_selectors_and_preserves_identity`
fails at line 405 because it still expects `<select id="recent-statistics-spa-id">`
on `/staff/photo-inventory`. The inventory selector markup was already modified
in the operator's dirty worktree before this session. Detector settings do not
change that page or assertion; retain this finding for the inventory UI owner.
