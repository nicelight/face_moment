---
description: Session papercuts recorded during TASK-060 execution.
status: active
---
# Session papercuts

- The task's documented Python probe command used `python`, but this host
  exposes only `python3`; the first probe stopped at shell setup with
  `command not found` and was discarded as claim evidence.
- The host `python3` environment lacks the project's SQLAlchemy dependency;
  the second host probe stopped at import setup and was discarded as claim
  evidence. Project-native Python checks use the backend Compose image.
- The exact Compose pytest gate emitted the existing `/app/.pytest_cache`
  permission warning; the suite still completed with `7 passed`, and the
  disposable databases/containers were cleaned up by the fixture and `--rm`.
