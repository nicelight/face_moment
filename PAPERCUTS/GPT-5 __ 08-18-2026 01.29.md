# Workflow papercut

- The required `Codex Luna` co-reviewer was unavailable for the current
  ChatGPT account; both mandated launches and both retries returned the CLI
  `400 invalid_request_error`.
- Two inline Node source probes initially failed because shell quoting escaped
  a regular expression incorrectly; the probes were rerun with simpler string
  checks and then passed.
- The read-only Docker pytest probe emitted a cache-write warning because the
  repository mount was intentionally read-only; the test itself passed.
