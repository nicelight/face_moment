# Papercuts

- `tests/diagnostics/test_attempt_investigation.py:224` rejects `calibration` in operator HTML, but shared navigation contains a hidden developer link. Result: 15 passed / 1 failed; reconcile the test and navigation in their owning scope.
- Version-1 search evidence records only threshold-valid matches. Actual Attempt `b4411f7a-1678-496c-b592-c0961a0c8835` has five quality-passing observations with empty matches, so neither the best rejected similarity nor historical eligible population can be recovered. The readable developer summary now explains this limitation; no threshold change or causal identity claim is supported.

- Fresh searches now persist pre-threshold best cosine and eligible Photo count; historical records remain incomplete. Twelve current-source tests passed.
- The source-mounted backend has image-baked tests; new tests require a disposable Compose run with the current `tests` mounted read-only.

- Reprocessing exposed an admission-revision default: display and phone readers returned four media 404s despite ready previews. Both now use the issuing Attempt's immutable revision; regression coverage requires it.
- Client `#debug` was a placeholder and discarded render errors. Added a bounded diagnostic log and safe error codes without request/credential data.

- Calibration's shared form script selected the first status output in the document. Scoped it to the submitted form and added bounded network/validation feedback.

- PromoDisplayController stored native `Window.fetch` without binding it; Chromium rejected configuration/ACK with `Illegal invocation`. Bound it to `globalThis` and added a default-fetch receiver regression.
