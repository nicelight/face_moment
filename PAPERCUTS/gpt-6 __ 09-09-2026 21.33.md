# Papercuts

- `tests/diagnostics/test_attempt_investigation.py:224` rejects `calibration` anywhere in operator HTML. The existing shared staff navigation includes a hidden developer-only `/staff/calibrations` link, so the diagnostic suite reports 15 passed / 1 failed before reaching the remaining assertions in that test. Current diagnostic detail still omits developer evidence for operator; live HTTP checks confirm it. The assertion and shared navigation need reconciliation in their owning scope.
- Version-1 search evidence records only threshold-valid matches. Actual Attempt `b4411f7a-1678-496c-b592-c0961a0c8835` has five quality-passing observations with empty matches, so neither the best rejected similarity nor historical eligible population can be recovered. The readable developer summary now explains this limitation; no threshold change or causal identity claim is supported.

- Follow-up resolves missing measurements for fresh searches: pre-threshold best cosine and eligible Photo count now travel from exact search to ordinary evidence and developer detail. Historical records remain missing by design. Twelve current-source tests passed.
- The running source-mounted backend still has image-baked tests. `docker exec ... pytest` cannot run newly added host tests; use a disposable Compose run with the current `tests` directory mounted read-only. The first new-test invocation stopped at collection with no tests run; the mounted rerun passed.

- Reprocessing exposed another admission-revision default: display and phone preview readers omitted the issuing revision. Actual successful camera results returned four media 404s because admission states were no_faces, although new ready previews existed. Corrected both callers to select the issuing Attempt's immutable revision; regression coverage now demands explicit issued revision. Existing search-only reprocessing proof did not cover media delivery.
- Client `#debug` was still a placeholder and onFailure discarded the specific render exception. Added a bounded local diagnostic log and safe machine error codes to make a future display failure observable without raw request/credential logging.

- Calibration's shared form script selected the first status output in the entire document. With the newly requested manual threshold form, unrelated form errors would appear in the threshold section. Scoped status output to the submitted form's section and added bounded network/validation feedback while implementing that UI.

- PromoDisplayController stored native Window.fetch without binding it. Chromium rejected configuration and ACK calls with Illegal invocation, while existing injected-arrow-fetch tests passed. The direct controller browser preview smoke also injected a wrapper and masked the native behavior. Bound fetch to globalThis (already used by SensorPassageClient) and added a default-fetch receiver regression; full application testing must preserve the native fetch binding and intercept only HTTP when fixtures are needed.
