# Papercuts

- Source-mounted Compose test still imports the installed `face_moment` package
  unless `PYTHONPATH=/app/src` is set; the initial focused test collected from
  the mounted path but could not import the new source module.
