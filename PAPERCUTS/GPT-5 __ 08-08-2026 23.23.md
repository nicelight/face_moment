- A first-pass `jq` filter treated `.memory-bank/tasks/index.json` as a top-level
  array, but the index wraps entries in `.tasks`; inspect the envelope or query
  `.tasks[]` before filtering task metadata.
- The environment has no `python` executable even though Python tooling is
  present; use the explicit `python3` command for read-only validation helpers.
- The base `python3` environment does not include `jsonschema`; task-card schema
  checks need the repository validator or a dependency-free schema probe.
- A final review-artifact `rg` check anchored only the field names and therefore
  missed lines containing their values; anchor the complete expected marker or
  avoid combining unrelated assertions in one regex.
- A custom FT-001 semantic check required the literal word `cleanup` even when
  the proof contract already described removal of only task-owned disposable
  state; keep discoverability assertions semantic or make the required term
  explicit before using a lexical guard.
