# Papercuts

- Codex CLI's `--ask-for-approval` is a global option and is rejected when placed after `codex exec`; the retry moved it before the subcommand.
- The unpinned Compose integration command selected a stale image that did not contain the task's focused test path; the task handoff supplies the pinned local image for the reproducible retry.
