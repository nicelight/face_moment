# Papercuts — kimi-k3 session

- `write` tool repeatedly failed with JSON truncation (`Unterminated string`)
  on payloads around 6-7 KB; smaller `edit` chunks on the existing file worked.
  Workaround: keep single `write` payloads modest or split via targeted edits.
- Docker Desktop on this host denies bind mounts from `/tmp/opencode`
  (`mounts denied ... not shared from the host`); only paths under the
  workspace (e.g. `/home/serg/Projects/face_moment/...`) are shared. Mount
  probe scripts from inside the repo instead.
- Parallel agent sessions can work on the same task concurrently: a second
  verifier wrote a `verify` entry and ran `/red-verify` while this session was
  mid-verification. Re-read task records immediately before editing them.
