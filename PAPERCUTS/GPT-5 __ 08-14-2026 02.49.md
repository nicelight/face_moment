# Papercuts

- A broad planning patch failed atomically because one boundary-map paragraph
  had drifted from the assumed wording; split the change into smaller,
  current-context patches after a targeted reread.
- An exact-substring consistency probe produced a false negative because the
  asserted Markdown sentence crossed a line wrap; normalize whitespace before
  checking prose anchors.
- Embedding Markdown backticks inside a template-literal wrapped validation
  command caused a wrapper parse failure; build multiline commands from quoted
  lines when validating backticked prose.
- A lifecycle probe assumed task cards lived inside `.tasks/<task>/task.json`;
  authoritative cards actually live as `.memory-bank/tasks/<task>.task.json`.
