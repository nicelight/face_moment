# Papercuts

- A compound preflight command stopped at an `rg` heading search whose exact casing did not match, hiding later read-only checks. Run heading discovery separately before chaining dependent context reads.
- A second compound source-inspection command used a stale guessed `processing/models.py` path and stopped before its remaining reads. Resolve paths with `rg --files` before composing dependent reads.
