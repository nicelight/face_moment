---
description: Session papercut log for fresh verification of TASK-047-T2-FT-003-W2.
status: active
---
# Papercuts

- `playwright cli run-code` does not expose the outer Node `setTimeout` global;
  the probe was corrected to use the browser's `window.setTimeout` inside
  `page.evaluate` and no repository/runtime state changed.
- A shell negative-scan command had an unmatched quote in its regex and exited
  before running the scan; it was corrected with a quote-free pattern and no
  repository/runtime state changed.
- One orchestration call had malformed JavaScript object syntax while closing
  the disposable browser session; the close was retried with a valid call.
- An `apply_patch` update used a stale protocol context line and failed
  verification; the exact current context was reread and the patch was
  retried without changing production or lifecycle state.
- The first crop fixture assertion used an incorrect center calculation, and
  the first EXIF fixture unintentionally placed the bbox at the source edge;
  both were corrected as task-local fixture expectations before the
  successful focused and browser runs.
