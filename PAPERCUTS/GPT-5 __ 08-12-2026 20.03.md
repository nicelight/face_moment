# Papercuts

- A broad task-context `rg` query exceeded the terminal output budget; use task-scoped reads and targeted section lookups for the remaining execution preflight.
- The packaged focused pytest suite passes but emits a `PytestCacheWarning` because the non-root `app` user cannot create `/app/.pytest_cache`; it does not affect the gate result.
