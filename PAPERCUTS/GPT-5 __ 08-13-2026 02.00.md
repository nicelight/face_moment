# Papercuts

- `docker compose run --rm --no-deps backend python -c 'from face_moment.processing.health_projection import ProcessingHealthProjectionRepository'` cannot reach the absent-module probe: the current image first raises an existing circular import through `processing.__init__` and `inventory.admission`. Use source-text absence evidence for the initial RED; a rebuilt current image is still required for exact packaged gates.
- The host exposes `python3` but not `python`; direct source-state probes must use `python3` even though the project’s container commands use `python`.
- `apply_patch` requires the literal final line `*** End Patch`; a malformed terminator fails before modifying the target file.
