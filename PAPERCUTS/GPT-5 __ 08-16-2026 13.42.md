---
description: Session papercut log for the TASK-047 executor run.
status: active
---
# Papercuts

- A shell composition-scan command failed once because nested quote escaping in
  the `rg` regular expression was invalid. No source or runtime state changed;
  the scan was narrowed and rerun with a simpler command.
- An attempted `playwright-cli` invocation used a non-existent executable name;
  the installed command is `/usr/local/bin/playwright cli`.
