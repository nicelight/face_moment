# Historical Memory Bank gate path

Existing completed FT-003 task cards and the baseline implementation plan use
`node scripts/mb-lint.mjs`, but that path is absent. The current deployed script
is `.memory-bank/scripts/mb-lint.mjs`; its local run passes. New follow-up cards
use the current path. Historical task evidence/status remains untouched.

# Existing bug-note metadata warning

Memory Bank lint passes but reports missing recommended `source_of_truth` in
`.memory-bank/bugs/task-101-calibration-missing-original-terminalization.md`.
This unrelated documentation warning does not block the FT-005 planning repair.
