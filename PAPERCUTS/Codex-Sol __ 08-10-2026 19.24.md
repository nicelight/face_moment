# Papercuts

- `docker compose run --rm backend alembic current` starts the `migrate`
  dependency unless `--no-deps` is supplied. Its stale image did not contain
  the database's already-recorded `0004_staff_sessions` revision, so the
  preflight command failed and left an orphan one-off container. For local
  migration inspection, rebuild first and use `--no-deps`.
