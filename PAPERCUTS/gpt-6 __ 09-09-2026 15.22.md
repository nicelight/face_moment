# Papercuts

- Strict mb-doctor reports TASK_ACCEPTANCE_EVIDENCE_MISSING for historical done TASK-106 / FT-011-AC-008; current task-specific review is APPROVE and default manual mb-doctor passes. Evidence: `.tasks/TASK-117-T2-FT-002-W7/preflight-doctor.json`. Preserve historical task evidence; this does not block independently reviewed manual TASK-117/118.

- `tests/processing/test_initial_pending.py` runs a migration downgrade against Settings.from_env().database_url directly. TASK-118 required gate must wrap this historical test in an outer disposable database; never run it against workstation/default data. Its `_add_photo` fixture also omits the required admission revision. Found by source inspection before execution.

- `processing/persistence.py:206` ExactCompatibleSearchRepository requires Photo.admission_pipeline_revision_id == query revision, incorrectly excluding new compatible revision states on previously admitted Photos. This contradicts canonical realtime-search scope and the requested preserved-lineage reprocessing result. TASK-118 needs a task-scoped search regression and removal of this extra read filter; no Photo lineage write is appropriate. Found before rollout, no current source change yet.

- TASK-118 repairs the evidenced initial-pending fixture hazard by giving that test its own disposable PostgreSQL lifecycle and supplying the required admission revision. The task's full legacy gate is additionally wrapped in a disposable outer database. The extra search admission filter is removed with a preserved-A/new-B regression.

- A source-only Docker rebuild re-downloads/builds Python dependencies: Dockerfile copies src before `pip wheel .` and disables pip cache. TASK-118 build log shows roughly six minutes before application/InsightFace wheels, delaying the local rollout. No Dockerfile change is needed for the accepted preprocessing outcome.
