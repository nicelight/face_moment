# Papercuts

- Strict mb-doctor flags missing acceptance evidence for historical TASK-106 /
  FT-011-AC-008; current TASK-117/118 review and default manual doctor pass.
  Evidence: `.tasks/TASK-117-T2-FT-002-W7/preflight-doctor.json`.

- `test_initial_pending.py` downgraded against the configured database and its
  fixture omitted admission revision. TASK-118 isolates it in a disposable DB.

- `ExactCompatibleSearchRepository` incorrectly filtered by admission revision,
  excluding compatible reprocessed Photos. TASK-118 removed the filter and added
  a preserved-A/new-B regression.

- TASK-118 gives the affected test a disposable PostgreSQL lifecycle and the
  required admission revision.

- Source-only Docker rebuilds redownload dependencies and delayed TASK-118 by
  about six minutes. No Dockerfile change was needed for this rollout.
