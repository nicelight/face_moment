# Papercuts

- A first-pass AWK validation assumed the `REQ` line immediately followed each
  feature AC heading, but the feature format inserts a blank line; validation
  needs to tolerate that spacing.
