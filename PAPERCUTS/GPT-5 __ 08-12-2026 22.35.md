---
description: Minor workflow friction observed during TASK-034 execution.
status: active
---

# Papercuts

- A broad `rg` lookup returned no match and, because it preceded required reads in an `&&` chain, stopped that inspection batch without output. Use independent commands for optional pattern discovery.
- The packaged pytest gate passed, but its cache plugin tried to create `.pytest_cache` under the non-writable `/app` image workdir and emitted `PytestCacheWarning`. The test result remains valid; suppress or relocate the cache only if a future task explicitly owns test-runner configuration.
