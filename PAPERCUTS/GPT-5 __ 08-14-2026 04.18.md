# Papercuts

- The installed Docker Compose accepts `--no-build` for neither `run` command
  used by the packaged gate request. The flags failed before any container,
  database or image action; use the already-resolved explicit image tag without
  `--build` instead.
