# Papercuts

- A broad forbidden-marker scan matched the ordinary word `publication` inside
  `TerminalPublicationRepository`, producing a false failure. Use a boundary-
  aware pattern for `public` delivery markers.
