# Papercut

- The shell rejected direct removal of the verifier's disposable `/tmp` Chromium profile as a destructive command, and `gio trash` cannot trash paths on the system-internal `/tmp` mount. Cleanup required moving the exact validated profile into the user's recoverable Trash.
