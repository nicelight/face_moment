# Papercuts

- System Ajv cannot compile the repository Draft 2020-12 task schema through
  its default constructor because that meta-schema is unavailable. Use the
  Draft 2020-specific entry point for local schema validation.
