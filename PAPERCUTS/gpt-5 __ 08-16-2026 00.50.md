- FT-003 task cards use the `*.task.json` suffix; assuming flat `*.json` paths caused an avoidable failed read before filesystem discovery.
- The abbreviated completed prerequisite `TASK-004` did not expose its wave; assuming `W1` caused the first lint run to fail because the indexed identity is `TASK-004-T3-FT-001-W2`.
- A custom normative-anchor audit initially collapsed adjacent spaces after an em dash and falsely rejected the repository's established double-`--` GitHub anchor form.
- A final `rg` pattern containing Markdown backticks was passed in shell double quotes, so bash attempted command substitution (`4`) before the read-only search.
- A patch used the pretty-printed `jq` view of compact JSON as literal context and failed verification even though the underlying task content had not changed.
- The installed finding-adjudication pack requires co-reviewers on `Codex Luna`, but the collaboration runtime exposes only `gpt-5.6-sol` and `gpt-5.6-terra`; both prescribed launch attempts fail before a reviewer starts.
- A read-only FT-003 structural audit accidentally serialized grouped task objects instead of numeric tier counts, producing heavily truncated output even though the final error list was empty.
