# Papercuts

- A batched read helper failed while assembling a multi-command response because one result was omitted from the destructuring list. Re-ran the required reads in smaller batches.
- The runtime image has PostgreSQL pgvector extension support but no Python `pgvector` package. The migration/model must use a minimal local SQLAlchemy vector type rather than an unavailable external import.
- The first disposable migration probe attempted to downgrade an unversioned database to `0007`; Alembic rejects that range. The focused test now upgrades directly to the required pre-cutover revision.
- A test-fixture migration-target edit retained an extra indentation level, causing pytest collection to fail. Inspected the surrounding fixture before correcting it.
- The available `face-moment:dev` image remains at Alembic head `0007_photo_pipeline_pending` while the working tree adds `0008`; mounted-source disposable tests were required until the build owner refreshes the image.
- The task-owned PostgreSQL probe used `docker run --rm`, so post-stop inspection reported no object rather than a stopped container. The removal is expected cleanup, but the verification command should account for `--rm`.
