# Papercut

- The installed `docker compose run` rejected the intuitive `--add-host`
  option before starting the disposable probe; use a separate Docker network
  or an image-native host alias for cross-container access.
- A `--rm` ephemeral PostgreSQL container removed itself on `docker stop`, so a
  follow-up explicit `docker rm` reported `No such container`; residue checks
  confirmed the container and published port were already absent.
