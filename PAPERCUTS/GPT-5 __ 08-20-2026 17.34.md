- `playwright cli open` writes page snapshots into the repository-local `.playwright-cli/` directory even when the task keeps its intended evidence elsewhere, leaving untracked cleanup artifacts after otherwise read-only browser session setup.
- The installed verification semantic pack requires `Codex Luna` co-reviewers, but the current collaboration runtime rejects that model as unavailable; both required focuses therefore fail even after the mandated single retry and cannot be substituted.
