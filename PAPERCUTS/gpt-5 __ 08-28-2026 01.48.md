# Papercuts

- `compose.yaml` interpolates `REALTIME_RESULT_DISPLAY_MS` and
  `REALTIME_SUCCESS_COOLDOWN_MS` as empty values when no `.env` is present;
  the `migrate` service then fails before task tests start. Task-local positive
  test overrides were used for verification; deployment configuration was not
  changed.
