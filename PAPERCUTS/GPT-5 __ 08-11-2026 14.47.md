# Papercuts

- The image-only Compose test starts a stale `migrate` image after a
  mounted-source migration advances the development database, so it fails before
  pytest can run because the old bundle cannot resolve the new revision. The
  scheduler must rebuild the image before that gate is usable.
