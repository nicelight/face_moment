---
description: Session papercuts recorded during TASK-057 execution.
status: active
---
# Session papercuts

- The repository task gate examples use `python`, but this shell exposes only
  `python3`; the first pre-change probe returned `command not found` and was
  discarded as setup friction rather than claim RED.
- The bare interpreter also lacks the editable package import path; the probe
  needed `PYTHONPATH=src`, and that setup failure was likewise discarded as RED.
- The initial Compose package image lagged the repository `HEAD`: an import
  failed there even though the task-owned adapter was already present in the
  source commit. The image was rebuilt before gates; the stale-image result is
  explicitly discarded as RED evidence.
- During the long package rebuild, `HEAD` advanced from `160e950` to
  `d5ff38f` via an external `FT-003 W1 PASS` commit. This uncontrolled
  background mutation is retained as evidence and prevents reuse-candidate
  claims for this attempt.
- A broad protocol patch failed because its expected context did not match the
  already-reconciled task files; no repository source or protocol file was
  changed by the failed patch.
- The first Attempt 2 RED command was rejected by the exec safety guard because
  its temporary-directory trap used `rm -rf`; no probe or repository mutation
  occurred. The probe was rewritten as a read-only git/tree check.
- The verifier HEAD-monitor wrapper was initially rejected because its
  ephemeral cleanup used `rm -f`; the retry leaves the mktemp file for the
  host's temporary-file cleanup instead.
- The fresh Docker pytest gate emitted a pre-existing permission warning for
  `/app/.pytest_cache`; all eight tests passed and no repository cache file was
  created or retained.
- The required `Codex Luna` co-review launch and its one retry were both
  rejected by the provider because that model is unavailable for the current
  ChatGPT account; direct verifier focus passes continued per the semantic
  pack's fallback rule.
- The first fresh red probe reached disposable PostgreSQL setup but its inline
  harness referenced an undeclared `_positive_error` helper; `finally` cleanup
  ran and the setup failure was discarded as probe friction, not product
  evidence.
