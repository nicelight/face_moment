# Papercuts

- The indexed Docker pytest command used an existing image that did not
  contain the untracked task-local test file, returning exit `4`; the bounded
  source-mounted recovery passed 4 tests. The image visibility mismatch remains
  an environment papercut and was not fixed in TASK-077.
- `node scripts/mb-lint.mjs` passes but retains pre-existing recommended
  `source_of_truth` frontmatter warnings for active FT-001, FT-002 and FT-003
  documents. They were recorded only; FT-003 was not edited.
