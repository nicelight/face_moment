# Motion Atlas integration

- Fluid mobile seams исправлены; доказательство: `.protocols/motion-atlas-qa/public-_site-mobile.png`.
- `/site` пока не выполняет public search/payment; это отдельный продуктовый объём.
- Исправлен CSS-селектор, перекрывавший цвет hero-кнопки.
- Uploader теперь объясняет JPEG rejection и proxy 413; покрыты validator-коды
  и не-JSON fallback. Read-only пример: 4608×3456, `decoded_side_exceeded`,
  EXIF orientation 3 допустим.
- YuNet без preprocessing дал `no_faces` на пяти из шести Photo. При рабочем
  размере 640 px повтор обнаружил все шесть; доказательство —
  `.protocols/model-debug/yunet-scale.log`. Это локальный sample, не общий SLO.
