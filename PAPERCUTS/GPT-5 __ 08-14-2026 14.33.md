Ajv is not resolvable from the project Node module graph; the direct task-schema validation command failed with ERR_MODULE_NOT_FOUND even though a system Ajv installation is present.

# Papercuts

- The native strict-doctor output is large enough for the shell tool to truncate; a direct in-process invocation was used to filter FT-001 findings without changing project state.
- The environment has `python3` but no `python` command; the first read-only schema-validation invocation used the unavailable alias and was rerun with the available interpreter.
- `python3` also lacks the optional `jsonschema` package; schema validation therefore used the repository's direct schema-shape checks plus native strict-doctor validation instead.
