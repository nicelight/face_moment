# Papercuts

- Docker-dependent task execution was unavailable because the Docker daemon socket `unix:///home/serg/.docker/desktop/docker.sock` was not running. This is an environment limitation; it did not mutate the repository or consume task retry capacity.
- The repository workflow examples use `python`, but this environment exposes no `python` executable; `python3` is the available interpreter candidate.
- The host `python3` environment has no `pytest` module, so the focused test cannot run outside the project container.
- The host does not expose the `alembic` CLI either; migration execution remains container-gated.
- The host `python3` environment also lacks project runtime dependencies such as SQLAlchemy; source-only checks are the only available local checks until Docker returns.
- A full Compose rebuild began re-downloading the heavy vision dependency layer
  from the stale `face-moment:dev` image and was stopped before completion; the
  task continued with a current-source bind-mounted overlay, with no retry
  budget impact.
