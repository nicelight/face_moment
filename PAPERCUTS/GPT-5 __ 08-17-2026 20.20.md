# Papercut

- Required fresh `Codex Luna` `xhigh` read-only co-review launches for both verification focuses were rejected twice by the current ChatGPT account with HTTP 400 (`Codex Luna` unsupported). No fallback model was used.
