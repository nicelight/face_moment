# Papercuts

- `rg --files` skips hidden task and Memory Bank paths unless `--hidden` is supplied; initial task-context discovery therefore returned only `AGENTS.md`.
- The repository has no root `tests/conftest.py`; runtime-test fixture discovery must start from the task-local test modules.
