- A first-pass FT-001 task summary assumed `runtime_context.write_boundary`
  and `runtime_context.forbidden_scope` were both arrays; current cards omit
  `write_boundary`, so nullable task-schema fields must be defaulted before
  applying `join` in compact `jq` reports.
- `node scripts/mb-doctor.mjs --json` reports every direct canonical SDD path
  containing a valid Markdown `#anchor` as missing because the fragment is
  included in the filesystem existence check. This produces misleading
  `TASK_SDD_SPEC_LINK_MISSING` and `TASK_HANDOFF_INCOMPLETE` warnings even
  though the files and anchors resolve and the current task-plan review has
  approved them.
- The configured Docker context is `desktop-linux`, but both its socket at
  `/home/serg/.docker/desktop/docker.sock` and the default
  `/var/run/docker.sock` are absent. Compose gates fail before build with an
  unavailable-daemon setup error, so the failure cannot serve as task RED.
- The first cold Compose build downloaded most of the single-release image
  dependencies for about 15 minutes, then failed at Pillow with a transient
  `files.pythonhosted.org` DNS-resolution error. Build output is extremely
  noisy under TTY progress and the late network failure discards the usable
  final image despite substantial cache warm-up.
- `docker compose run` successfully created the disposable stack and test
  container but then returned `unexpected EOF` before starting that container.
  Inspecting the exact created container and running `docker start -a <name>`
  recovered the intended command without recreating state; Docker Desktop's
  attach/command completion also remained unusually slow after container exit.
- The host shell has no `python` executable, so otherwise harmless host-only
  JSON/syntax convenience checks fail with exit 127. Project Python gates must
  use the configured target image; non-Python JSON validation can use Node.
- Concurrent Codex sessions can mutate the same task verification artifact and
  stop Docker Desktop between commands. Use a task-attempt-unique Compose
  project/image, re-read overlapping protocol/task files immediately before
  patching, and never clean another session's disposable resources.
- One orchestration read failed before execution because a hand-written
  `functions.exec` JavaScript object contained a mismatched quote in
  `yield_time_ms`. The retry was side-effect free; prefer minimal copied call
  shapes for read-only inspection.
