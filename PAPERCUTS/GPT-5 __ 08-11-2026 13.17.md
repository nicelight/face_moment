- A combined `apply_patch` for the resumed task protocol failed because the
  handoff context did not match exactly; inspect the local artifact and apply
  smaller context-specific updates.
- A completed long-running command yielded only Compose container creation, and
  its follow-up poll reported an unknown cell; rerun the bounded check to obtain
  durable observable output.
