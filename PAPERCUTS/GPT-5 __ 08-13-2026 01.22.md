- Однострочный `docker compose run ... python -c` для read-only cleanup audit
  завершился в host shell с `unexpected EOF` из-за вложенной SQL quote до
  запуска контейнера. Для многострочных verifier probes безопаснее передавать
  Python через stdin (`python -`) с literal heredoc.
