# Papercuts

- В `SERVER/serverparams.md` обнаружены повторяющиеся строки таблиц
  `Виртуализация` и `UID/GID` для пользователя `display`. Они не относятся к
  переходу с IPsec на FRP и в рамках текущего изменения не исправлялись.
