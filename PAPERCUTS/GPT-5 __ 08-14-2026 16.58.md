# Papercut

- First task-card inspection command constructed duplicate `T030`-style ID segments and failed to open the target JSON files; retried using exact filenames from `.memory-bank/tasks/index.json`.
- Final summary probe incorrectly read `depends_on` from `.memory-bank/tasks/index.json` entries (which contain only `id`/`file`); the authoritative task cards and doctor checks were unaffected.
- Packaged verification build was fully uncached and spent roughly 14 minutes
  downloading the Docker toolchain/dependencies before producing the local
  `face-moment:task-039` image; source-mounted gates were available while it
  ran.
- Host `python` is unavailable; a final read-only JSON sanity command needed
  the project-available `python3` executable instead. No task gate or artifact
  was affected.
- A broad recursive preflight search exceeded the output budget and was
  truncated; subsequent reads were narrowed to the exact task, spec anchors,
  and source files. No evidence or source state was affected.
- A first serving-control RED probe imported `serving_control` cold and hit an
  existing `processing` package circular import before reaching the claim;
  the probe was reordered to the established processing-first import basis.
  No production code was changed for this unrelated prerequisite.
- Docker Compose v5 rejected a safe `--no-build` placement before `up`; the
  command had made no state change and was retried with the subcommand-scoped
  flag. No image build was invoked.
- The disposable base-DB migration command applied PostgreSQL migrations but
  then failed its unrelated MinIO bucket check because only `postgres` was
  running. The DB-only admission regression was rerun without that bucket
  dependency; no production state was used.
- Both required Codex Luna co-review launches and their single retries were
  rejected by the ChatGPT-account provider; the independent review continued
  after the prescribed retry limit.
- A read-only `jq -e empty` sanity probe reported every valid JSON task card as
  invalid because `empty` emits no result and exits non-zero; JSON parsing was
  separately confirmed with `jq` status extraction, and no task artifact was
  affected.
- A first local anchor probe treated stable `FT-<NNN>-AC-<NNN>` claim locators
  and the repository's doubled-hyphen Architecture Spine fragments as literal
  GitHub slugs; the focused validator was adjusted to honor those project
  locator conventions, with no source or task artifact change.
