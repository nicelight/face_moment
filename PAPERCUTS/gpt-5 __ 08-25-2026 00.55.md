# Papercuts

- `node scripts/mb-lint.mjs` passes but reports that
  `.memory-bank/features/FT-001.md` and `.memory-bank/features/FT-002.md` lack
  the recommended `source_of_truth` frontmatter used by active documents.
- Tier policy recognizes and preserves pre-implementation GREEN, but
  `scripts/mb-doctor/acceptance-trace.mjs` has no equivalent prospective form:
  it accepts only `RED` plus `GREEN` or `RED_NOT_APPLICABLE` plus alternative
  proof. A `PRE_IMPLEMENTATION_GREEN:` task marker is therefore rejected even
  though the policy names that state.
