# Papercuts

- The repository/tool shell has no `python` executable, so a first schema-validation probe failed before switching to an available runtime.
- An unquoted backtick in an `rg` pattern triggered shell command substitution (`4: command not found`) even though the surrounding validation command still exited successfully; quote literal Markdown backticks in shell patterns.
- `python3` is present but the `jsonschema` module is absent, so direct local Draft 2020-12 validation needs the repository lint or a small schema-aware fallback rather than the obvious library.
