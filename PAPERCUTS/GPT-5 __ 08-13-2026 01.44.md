# Papercuts

- Initial TASK-027 post-gate runtime audit used `processing_runtime_status.id`; the singleton key is `singleton_id`. The SQL was read-only and failed before the runtime check; inspect the schema before rerunning.
