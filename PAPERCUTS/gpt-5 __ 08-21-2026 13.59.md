# Papercuts

- The first task-local Playwright probe had one missing `Object.fromEntries`
  closing parenthesis, so the initial command stopped at JavaScript parsing and
  produced no valid claim RED. The syntax was corrected before rerunning the
  pre-implementation probe or changing production behavior.
- The repository intentionally has no local Node package manifest while the
  accepted browser workflow uses the globally installed Playwright CLI. An ESM
  bare import could not resolve that global package, so the focused probe was
  switched to `createRequire` and an explicit non-secret `NODE_PATH` qualifier.
- The `/verify` finding-adjudication pack requires two fresh `Codex Luna`
  co-reviewers, but the collaboration runtime exposes only `gpt-5.6-sol` and
  `gpt-5.6-terra`; both mandated focuses were retried once and rejected before
  launch, and the pack forbids substituting another model.
- A point-of-use Node preflight was first passed through a double-quoted shell
  command containing a JavaScript template literal, so Bash evaluated its
  backticks and the check stopped at parsing; the preflight was rerun safely
  through a single-quoted heredoc and passed.
- The first verifier-owned browser probe resolved its project root with one
  excess parent segment and stopped on `ENOENT` before browser launch; only the
  evidence probe path was corrected before rerunning it.
- The next verifier-probe setup assertion compared a sensor host with its raw
  input even though the real reader canonically prefixes `http://`; the
  expected normalized value was corrected before the decisive rerun.
- The first adversarial `SIGKILL` probe wrote synthetic managed configuration
  only 250 ms before killing Chromium, which did not match the runbook's
  preconfigured-profile precondition and lost the just-created profile data;
  the decisive probe first durably established configuration in a separate
  clean browser session, then crashed the subsequent participant session.
- The first red-probe residue check matched its own shell command because the
  command line contained the exact temporary-profile marker; it was rerun with
  a self-excluding Chromium/profile pattern.
