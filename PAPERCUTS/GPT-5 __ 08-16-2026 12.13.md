## Papercuts

- An `apply_patch` update to the new TASK-065 progress file initially failed
  because the expected wrapped line did not match the template-generated text;
  the file was inspected and patched with exact context.
- Ubuntu's system `mawk` rejected the initial `awk match(..., array)` syntax in
  the new topology checker. The parser was rewritten to POSIX field parsing and
  the corrected checker was rerun; no credentials or host state were touched.
