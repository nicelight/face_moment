---
description: Minor workflow friction observed during the TASK-049 verification session.
status: active
---
# Session papercuts

- `scripts/check-kiosk-browser.sh` probes the HTTPS origin with `HEAD`; this
  host returns `405 Allow: GET` while a redacted `GET` returns `200`, so the
  checker emits a potentially misleading origin `NOT_AVAILABLE` result. The
  verification report preserves both observations and does not use the HEAD
  result as proof of origin outage.
- `systemd-analyze verify` also emitted unrelated host-unit deprecation
  warnings; the task-relevant result was the missing `/usr/bin/chromium`
  executable.
