# Papercuts

- `docker compose run` запускает pytest от непривилегированного пользователя в
  `/app`, поэтому стандартный `.pytest_cache` не создаётся; для чистого вывода
  передавать `-o cache_dir=/tmp/pytest-cache`.
