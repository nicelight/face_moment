# Papercuts

- Dockerized focused pytest gate emitted a non-blocking `PytestCacheWarning`
  because `/app` is not writable for `.pytest_cache`; the focused suite still
  completed with `2 passed` and exit code `0`.
