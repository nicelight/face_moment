---
description: Minor workflow friction observed during the TASK-027 independent verification session.
---

- A context-read command used the literal shell glob `.memory-bank/guides/*.md`;
  because that directory has no matching files, `rg` returned an error and the
  following `&&`-chained reads did not execute. Prefer `rg --files` discovery or
  omit absent optional context directories before composing the read command.
