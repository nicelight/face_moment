# Papercuts

- The installed Ajv accepts the task schema but prints a deprecation warning
  for its `jsonPointers` option; omit that optional formatter setting in future
  local schema checks.
- A multi-file patch failed because one long context block did not match even
  though the intended lines were nearby; smaller targeted hunks applied cleanly.
