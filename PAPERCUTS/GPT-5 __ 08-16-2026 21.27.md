<!-- Session papercut log. -->
- Disposable verification fixture volume was created root-owned, while the current backend image runs as `app`; the first seed stopped with `PermissionError` before writing fixture files. Recreated the disposable state with an explicit root-only setup step; no implementation or shared service was affected.
- The first disposable edge probe used the container hostname as TLS SNI while the Caddy site is `localhost`; Caddy rejected the handshake before HTTP. The probe was adjusted to connect to the edge address with `localhost` SNI; no service or source configuration was changed.
- Minimal Caddy image does not include `rg`; the first adapted-config inspection returned that tool error after Caddy itself adapted successfully. Repeated the filter on the host without changing the Caddy config.
- The first orchestrated probe was rejected by the JavaScript tool wrapper because shell array interpolation was not escaped; no command or disposable resource started. Rebuilt the command with an environment wrapper.
- The first completed probe harness had an unmatched list-comprehension bracket in its non-secret fixture log scanner; runtime containers and volumes were cleaned by the exit trap, then the probe was rerun with the scanner fixed.
