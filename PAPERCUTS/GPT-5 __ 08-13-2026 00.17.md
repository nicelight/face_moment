# Papercuts

- A broad multi-document preflight read was truncated by the command-output limit; use smaller, targeted reads for complete normative context.
- An assumed standalone dependency-graph file did not exist; the canonical module dependency graph is in `.memory-bank/contracts/boundary-map.md`.
- An assumed `.memory-bank/rtm.md` route is absent; REQ traceability is maintained in `.memory-bank/requirements.md`.
- A large protocol initialization patch failed validation because one added line lacked the patch prefix; split task-owned protocol writes into smaller patches.
