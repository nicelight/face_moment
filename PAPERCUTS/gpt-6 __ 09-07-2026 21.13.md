# TASK-106 execution papercuts

- `python` is absent in the host shell; use `python3` for host bookkeeping. Initial failed bookkeeping was followed by Docker fixture image download before claim; corrected before all probes and production edits. Keep dependent setup sequential after checking bookkeeping result.
- The unpinned `pgvector/pgvector:pg16` alias was not cached; used existing `pgvector/pgvector:0.8.0-pg16` for the isolated fixture. Default containers were untouched.
- Existing Alembic configuration emits the `path_separator` deprecation warning during disposable migrations; tests still pass.
