# Papercuts

- A mounted source checkout does not override the installed package in the reusable backend image without `PYTHONPATH=/app/src`; the first post-change probe therefore exercised the stale image source and repeated the expected `404`.
