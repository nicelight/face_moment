# Workflow papercuts

- Temporary multipart fixture generator mixed `str` and `bytes`; the failed
  command produced no application or database mutation and was corrected
  before verification continued.
- A task-owned verifier probe initially used one extra parent-directory level
  in its relative import; the failed read-only Node run was corrected and the
  probe passed without application or database mutation.
