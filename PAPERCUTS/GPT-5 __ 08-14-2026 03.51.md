- A verifier-owned packaged ASGI matrix tried to restore a monkeypatched
  production callable through the imported test module, where that symbol is
  not exported. Preserve the callable from the production module before the
  injection; the disposable fixture's `finally` teardown still removed its
  database and `docker compose run --rm` removed the probe container.
