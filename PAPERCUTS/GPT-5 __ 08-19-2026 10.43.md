# Papercut

- Initial context search included two absent optional directories (`.memory-bank/requirements` and `.memory-bank/specs`); `rg` reported non-fatal path errors, then the available canonical paths were used.
- One verifier probe command had a malformed JavaScript tool-call object; it was corrected and rerun successfully.
- The Compose browser probe served the stale `face-moment:manual-20260818` image, so the delivered client bundle did not include the current production wiring. Source-current runtime must be rebuilt or served separately before treating the browser result as task evidence.
- The first fresh verifier production-probe invocation omitted the required Playwright `open` step; the session was opened and the same probe was rerun.
