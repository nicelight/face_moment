# Papercuts

- `npx --yes @mermaid-js/mermaid-cli --version` spent several minutes resolving and downloading the full renderer/Chromium dependency tree without reaching the version output, so syntax-only validation needs a lightweight installed parser or a project-local Mermaid check.
