# Papercuts

- The exact QR API gate passes, but its one-off backend container emits
  `PytestCacheWarning` because the runtime user cannot create `.pytest_cache`
  under `/app`. This does not affect test results; the project gate owner can
  later route pytest cache to a writable disposable path or disable the cache
  provider.
