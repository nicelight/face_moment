# Наблюдения при исправлении realtime timeout

- Полный `npm run test:unit`: 77 passed, 3 failed. Все три падения в
  `tests/client/test_capture_completion_identity.mjs`: VM fixture не предоставляет
  `clearPromoHandoff`, который вызывает извлечённый обработчик `attempt-start`
  из `client/app.js`. В `c23908f` уже присутствуют и этот вызов, и неполный
  fixture; проблема предшествует watchdog. Для проверки затронутого capture
  recovery fixture дополнен отсутствующими UI-зависимостями.
- PostgreSQL из `.env.local` недоступен на `127.0.0.1:55432`; текущий Compose
  PostgreSQL не публикует порт. Для проверки использована отдельная временная
  БД на `127.0.0.1:55439`, без изменения рабочих volumes и конфигурации стенда.
- В startup-recovery fixture использовался неполный `SimpleNamespace` настроек
  без `database_url`, хотя lifecycle уже создаёт diagnostics binding. Для
  проверки сохранённого restart recovery fixture использует реальные Settings
  из изолированной среды с заменой двух display timing значений.

После обновления fixtures: весь клиентский набор — 80 passed; startup-recovery
проверка — passed. Рабочие сервисы и их данные не изменялись.
