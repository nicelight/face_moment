# Session papercuts

- The first verifier browser probe waited for
  `document.body.dataset.attemptState === "advertising"` immediately after
  reload, but the client exposes the advertising view before that dataset is
  initialized by a lifecycle transition. The wait timed out without reaching
  a production assertion; using the rendered `.advertising-card` as initial
  readiness made the final disposable run deterministic.
