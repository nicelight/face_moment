# Papercuts

- A grouped `wc` preflight omitted `.md` on four protocol paths, so `&&` prevented the intended policy read. Use explicit complete filenames when combining validation reads.
- A large multi-file evidence patch had one unprefixed wrapped line and failed validation. Split protocol/evidence updates into smaller patches.
