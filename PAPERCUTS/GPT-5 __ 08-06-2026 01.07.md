# Papercut

- The host environment has no `python` executable; schema validation must use `python3` or the project container, while task gate commands run `python` inside Docker.
- `python3` is available but the host lacks the `jsonschema` module, so host-side schema validation needs the project validator/container fallback.
- The host `ajv` package is an older draft implementation and cannot load the task schema's Draft 2020-12 meta-schema; `node scripts/mb-lint.mjs` remains the available project-native structural validator.
- A first custom task-reference validator falsely reported valid Markdown anchors because its heading normalization escaped the generated-anchor pattern; exact heading slug extraction is required for reliable bounded reference checks.
- `AGENTS.md` now consolidates the KISS gate under `Reasoning Policy: Selection Before Expansion (KISS Gate)`, while `.memory-bank/roles/architect.md` still names the removed `AGENTS.md#KISS / Complexity and Requirement Gate` heading; the semantics remain readable but the role pointer is stale.
- A broad final `jq` reread exceeded the model output budget despite a narrow intent; split field-level selectors keep planning-validation output bounded.
