# Papercuts

- `multi_agent_v1__spawn_agent` rejected a valid-looking launch because the tool accepts either `message` or `items`, not both; retrying with a plain message.
- `codex exec` rejected the documented-looking `--ask-for-approval` flag; retrying with the supported config override for `approval_policy`.
- A fresh `/exe TASK-062-T2-FT-003-W2` Implementer remained `running` for several minutes without creating the required `.tasks/` or `.protocols/` initialization artifacts; it was shut down and the selected task was handed to a fresh worker.
- A first sync-local validation snippet assumed `.memory-bank/tasks/index.json` was an array; the authoritative file is an object containing `tasks`, so the read-only check was corrected and rerun.
- The host `python3` environment has no `pytest` module, so the central-shell
  regression must run in the rebuilt project image; the host result is not
  treated as task evidence.
- The first compose inspection used the absent `docker-compose.yml` name; the
  project uses `compose.yaml`, so the read-only inspection was retried there.
