# Papercuts

- The host exposes `python3` but not the `python` alias; its Python 3.14 environment also lacks the project pytest/mypy dependencies, so focused source checks require the project image after its owner rebuilds it.
- A bulk protocol/evidence patch had one stale context line and was rejected without applying; split the update into smaller patches after re-reading the current protocol files.
