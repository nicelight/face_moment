# Papercuts

- A focused adapter fixture gained a required configured embedding-dimension field; an initially incomplete negative fixture and a follow-up duplicated keyword caused two non-claim GREEN setup failures before correction.
- The host shell has no `python` alias; use `python3` for local JSON/status probes.
- An evidence patch initially missed the current protocol wording; inspect before applying a context-sensitive patch.
- A one-line mypy `-c` probe passed literal newline escapes rather than source newlines; use shell ANSI-C quoting for multiline inline probes.
- A forbidden-mechanism scan must distinguish the required explicit `download=False` argument from a model-download capability.
