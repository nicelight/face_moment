# Session papercuts

- Playwright CLI central-origin probe initially stalled because the running
  `face-moment:dev` backend image did not mount the current workspace source;
  the edge served the pre-change client, so the expected notice DOM was absent.
  The probe was rerouted to a disposable local static server using the current
  source snapshot.
- The optional Python client regression gate was unavailable because neither
  `pytest` nor the `pytest` Python module is installed in the environment.
