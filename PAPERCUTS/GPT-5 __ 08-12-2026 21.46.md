- `docker compose config --format json` serializes service-network membership as an object whose present network values are `null`; checking `.services.<service>.networks.<network> != null` therefore reports false even though the network is configured. Check key membership with `has("<network>")` instead.
