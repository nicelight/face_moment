connections {
    face-moment-backhaul {
        version = 2
        local_addrs = 46.8.200.99
        remote_addrs = %any
        proposals = aes256gcm16-prfsha384-ecp384
        fragmentation = yes
        mobike = yes
        dpd_delay = 15s
        send_cert = always
        unique = replace

        local {
            auth = pubkey
            certs = vps-cert.pem
            id = vps.face-moment.internal
        }

        remote {
            auth = pubkey
            id = central.face-moment.internal
        }

        children {
            backhaul {
                local_ts = 10.77.0.1/32
                remote_ts = 10.77.0.2/32
                if_id_in = 42
                if_id_out = 42
                esp_proposals = aes256gcm16-ecp384
                start_action = none
                dpd_action = clear
                close_action = none
            }
        }
    }
}
