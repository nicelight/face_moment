---
description: Feature FT-011 — explainable threshold and input-quality Calibration.
status: draft
lifecycle: planned
last_updated: 2026-08-01
clarification_status: complete
last_clarified: 2026-08-01
clarification_questions: 1
spec_design_status: complete
spec_design_links:
  - .memory-bank/spec-backbone.md
  - .memory-bank/architecture/system-architecture.md
  - .memory-bank/contracts/boundary-map.md
  - .memory-bank/states/lifecycle-map.md
  - .memory-bank/testing/index.md
  - .memory-bank/testing/calibration.md
---
# FT-011 — Explainable Calibration

**Epic:** [EP-003 — Explainable Diagnostics and Calibration](../epics/EP-003.md)

## Outcome

An authorized developer can compare face pipelines and parameter sets, inspect
explainable threshold and one-dimensional input-quality recommendations, and
then decide separately whether to apply a serving-setting change manually.

## Requirements

- `REQ-CAL-001`, `REQ-CAL-002`, `REQ-CAL-003`.
- Cross-cutting: `REQ-DATA-001`, `REQ-ARCH-001`.
- Registry: [.memory-bank/requirements.md](../requirements.md).

## Use Cases

- Compare SFace and Buffalo M on annotated pilot attempts.
- Inspect `Best face match`, `Balance` and `Minimum missed faces` threshold
  profiles and drill down to contributing attempts.
- Analyze face size, detection confidence, blur, brightness and pose gates one at
  a time.
- Compare before/after release or parameter-set results and manually apply a
  selected serving change through the later audited boundary.
- Run Calibration on the shared background worker and manually rerun an
  interrupted analysis after photo processing has resumed.

## Acceptance Criteria

### FT-011-AC-001 — Explainable threshold profiles

- REQ: REQ-CAL-001
- Observable criterion: Calibration compares SFace and Buffalo M and presents
  `Best face match`, `Balance` and `Minimum missed faces`; every recommendation
  shows proposed threshold, correct/false/missed counts, precision, recall,
  annotated sample size and contributing-Attempt drill-down. The first and third
  profiles obey their PRD tie-breaks. `Balance` maximizes aggregate
  `F1 = 2 * correct / (2 * correct + false + missed)`; equal F1 is resolved by
  fewer `false`, then fewer `missed`, then the higher (stricter) threshold.
- Verification: Calculate all profiles over a fixed annotated sample, compare
  metrics/drill-down with an independent calculation and assert the accepted
  `Balance` objective and complete tie-break order.

### FT-011-AC-002 — One-dimensional quality recommendations

- REQ: REQ-CAL-002
- Observable criterion: Face size, detection confidence, blur, brightness and
  pose are analysed one at a time; each output shows current/proposed value,
  sample size, kept/rejected detections and expected correct/false/missed
  changes, without joint multidimensional optimization.
- Verification: Vary each gate independently on one annotated fixture set and
  assert output fields plus unchanged values for all other gates.

### FT-011-AC-003 — Reproducible before/after comparison

- REQ: REQ-CAL-002
- Observable criterion: Stored releases, parameter sets, outcomes and
  annotations support before/after comparison and contributing-Attempt
  inspection without a separate experimentation platform.
- Verification: Compare two stored release/configuration fixtures and reconcile
  aggregate changes with the underlying annotated Attempts.

### FT-011-AC-004 — Manual-only serving change

- REQ: REQ-CAL-002
- Observable criterion: A recommendation never mutates serving settings
  automatically; any accepted change requires a separate explicit developer
  action through the serving-control boundary.
- Verification: Generate and inspect recommendations while asserting unchanged
  serving state, then verify only the separately authorized command can change
  it.

### FT-011-AC-005 — Shared-worker interruption recovery

- REQ: REQ-CAL-003, REQ-ARCH-001
- Observable criterion: Calibration may delay photo processing on the shared
  worker during debugging; worker restart makes the run visibly `failed` or
  `interrupted`, resumes queued Photo work and leaves rerun as a manual action
  without preemption, priority scheduling or a separate Calibration worker.
- Verification: Restart during Calibration with queued Photo work and inspect
  run terminal state, Photo processing resumption and absence of automatic
  replacement execution.

### FT-011-AC-006 — Honest sample and promoted-case retention

- REQ: REQ-DATA-001, REQ-CAL-001, REQ-CAL-002
- Observable criterion: Insufficient annotated evidence remains explicit in
  sample size and cannot trigger an automatic change; promoted cases retain
  only the curated PRD subset until explicit deletion.
- Verification: Run calculations below an adequate sample and apply retention
  to ordinary/promoted cases while inspecting output warnings and surviving
  evidence.

## Edge and Failure Behavior

- Insufficient annotated evidence remains visible in sample size and cannot be
  hidden by an automatic serving change. Covered by `FT-011-AC-004` and
  `FT-011-AC-006`.
- Capture-window, frame-interval, group-selection, CPU/thread and UI/QR timing
  tuning remain outside the first Calibration scope.
  - Disposition: out_of_scope
  - Source: PRD `Non-goals` and `FR-DEV-08`.
  - Change route: /write-prd
- Promoted cases retain only the curated PRD subset until explicit deletion.
  Covered by `FT-011-AC-006`.
- No preemption, priority scheduler, automatic Calibration recovery or separate
  Calibration worker is required; the developer starts a replacement run.
  Covered by `FT-011-AC-005`.

## Inputs and Evidence

- [.memory-bank/prd.md](../prd.md): FR-DEV-05..11, NFR-PERF-03,
  NFR-DATA-03 and AC-11..13.
- [.memory-bank/invariants.md](../invariants.md): manual-only application and
  no speculative tuning expansion.
- [.memory-bank/contracts/boundary-map.md](../contracts/boundary-map.md):
  Calibration-to-serving boundary and canonical retention cleanup contract.
- [IDEA_DEBUG.md](../../IDEA_DEBUG.md): supporting profile, quality-gate and
  comparison evidence under PRD precedence.

## Verification Targets

- `FT-011-AC-001..006`, preserving PRD AC-11, AC-12 and AC-13.
- Recommendation output is explainable and serving configuration remains
  unchanged until a separate explicit developer action.
- Worker-interruption evidence shows Calibration terminal state, resumed photo
  processing and manual rerun behavior.

## Clarifications

### 2026-08-01 — Deterministic `Balance` objective

- Operator answer: maximize aggregate
  `F1 = 2 * correct / (2 * correct + false + missed)`.
- Deterministic tie-break order: fewer `false`, then fewer `missed`, then the
  higher (stricter) threshold.
- Consequence: `FT-011-AC-001` and its verification now have a deterministic
  pass/fail rule. Annotation semantics, sample selection, the other threshold
  profiles, quality-gate analysis and manual-only serving application are
  unchanged.
- Design impact: reconciled in the canonical
  [.memory-bank/testing/calibration.md](../testing/calibration.md) verification
  contract; behavior-spec refresh is not required because FT-011 has no linked
  behavior JSON.

## SDD Design Gate

Complete. The global Architecture Spine and capability ownership are reused
from the system architecture and boundary map; shared-worker, interruption and
retention behavior reuse the lifecycle map. The canonical
[Calibration verification](../testing/calibration.md) contract owns the fixed
evaluation inputs, accepted `Balance` F1 oracle, deterministic profile proof,
one-dimensional quality analysis, before/after comparison, manual-apply
isolation and recovery/retention evidence. No global rule changed, so Planning
Revision `4` remains current. Task planning was intentionally not performed.
