services:
  postgres:
    ports:
      - "127.0.0.1:${FACE_MOMENT_LOCAL_POSTGRES_PORT:-55432}:5432"
    networks:
      - private
      - local

  minio:
    ports:
      - "127.0.0.1:${FACE_MOMENT_LOCAL_MINIO_PORT:-59000}:9000"
    networks:
      - private
      - local

networks:
  local:
